apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Backward-compatibility alias for the OpenShift with-pull-secret overlay.
# The canonical location is `k8s/overlays/openshift/with-pull-secret`.
resources:
  - ../openshift/with-pull-secret
