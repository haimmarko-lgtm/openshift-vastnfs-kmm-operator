apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Backward-compatibility alias for the OpenShift secure-boot overlay.
# The canonical location is `k8s/overlays/openshift/secure-boot`.
resources:
  - ../openshift/secure-boot
